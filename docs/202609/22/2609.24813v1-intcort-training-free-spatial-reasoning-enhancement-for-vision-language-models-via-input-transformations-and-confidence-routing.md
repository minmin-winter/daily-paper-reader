---
title: "INTCORT: Training-Free Spatial Reasoning Enhancement for Vision-Language Models via Input Transformations and Confidence Routing"
title_zh: INTCORT：通过输入变换与置信度路由实现视觉语言模型的免训练空间推理增强
authors: "Haoran Sun, Jingqi Xu, Yanhui Li, Enci Liu, Kaidi Xu, Yanwei Liu"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24813v1"
tags: ["query:mm-reasoning"]
score: 8.0
evidence: 增强视觉语言模型的空间推理
tldr: "视觉语言模型在空间推理上仍较弱，现有增强方法或需高成本训练并导致灾难性遗忘，或干扰模型内部机制损害通用能力。INTCORT 提出免训练框架，通过几何图像变换与查询反转构造多推理视图，并利用关系 token 置信度路由聚合预测，不改动 VLM 内部结构。在多个常用基准与多种 VLM 上，空间推理准确率平均提升 10.01%，相较已有方法最高提升 25.01%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24813v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1651, \"height\": 690, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.24813v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 140, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24813v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 858, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24813v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 807, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24813v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1798, \"height\": 1071, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24813v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 573, \"height\": 182, \"label\": \"Table\"}]"
motivation: VLM 空间推理能力弱；训练型方法成本高且易灾难性遗忘，免训练方法常干扰内部机制、损害通用能力。
method: INTCORT 免训练：用几何图像变换与查询反转生成多推理视图，再按关系 token 置信度路由聚合预测，不改内部机制。
result: "多个基准和多种 VLM 上平均提升 10.01%，相比先前工作最高提升 25.01%，验证有效性。"
conclusion: 证明免训练输入变换与置信度路由可稳定增强空间推理，且无需改模型，具备跨 VLM 通用性与实用价值。
---

## 摘要
视觉语言模型（VLMs）在多模态任务中已展现出卓越能力，但在空间推理方面仍表现不佳。现有的依赖训练和免训练增强方法分别面临高计算成本且伴随灾难性遗忘，以及内部机制干扰损害通用能力的问题。在这项工作中，我们首先验证了两个关键假设：适当的几何图像变换和查询反转变换能够纠正错误的空间预测，且正确预测比错误预测表现出更高的关系标记置信度。基于这些发现，我们提出了 INTCORT，一个免训练的空间推理增强框架，它通过输入变换构建多个推理视图，并利用关系标记置信度路由聚合其预测，而无需修改 VLM 的内部机制。在若干常用基准上的实验结果表明，INTCORT 在多种 VLM 上显著提高了空间推理准确率，在所有模型和基准上平均提升 10.01%。与先前工作相比，INTCORT 取得了更优性能，提升最高达 25.01%。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable capabilities in multimodal tasks, yet they still exhibit poor ability in spatial reasoning. Existing training-dependent and training-free enhancement methods suffer from high computational costs with catastrophic forgetting and internal mechanism interference that compromises general capabilities, respectively. In this work, we first verify two key hypotheses: appropriate geometric image transformation and query-reversal transformation can recover incorrect spatial predictions, and correct predictions exhibit higher relation-token confidence than incorrect ones. Based on these findings, we propose INTCORT, a training-free spatial reasoning enhancement framework that constructs multiple inference views through input transformations and aggregates their predictions via relation-token confidence routing, without modifying the VLM's internal mechanisms. Experimental results on several commonly-used benchmarks demonstrate that INTCORT substantially improves spatial reasoning accuracy across diverse VLMs, achieving an average improvement of 10.01% over all models and benchmarks. Compared with prior works, INTCORT achieves superior performance with improvements of up to 25.01%.

---

## 论文详细总结（自动生成）

# INTCORT 论文中文总结

## 1. 核心问题与整体含义
- **研究动机**：视觉语言模型（VLM）在多模态任务中表现强大，但在空间推理（如 left/right、above/below、实体间相对位置）上仍明显不足。该能力对具身智能、机器人导航和场景理解至关重要。
- **现有方法局限**：
  - **训练依赖方法**：依赖大规模空间推理数据微调，计算成本高，且可能引发灾难性遗忘。
  - **免训练方法**：多通过注意力干预或解码策略（如 AdaptVis、DoLa、VCD）干预模型内部机制，可能损害 VLM 在其他推理任务上的原始能力。
- **整体含义**：论文提出一种**输入侧、免训练**的空间推理增强框架 INTCORT，不修改 VLM 内部结构和参数，通过多推理视图与置信度路由提升空间推理，同时尽量避免损害通用能力。

## 2. 方法论
### 2.1 核心思想
- 论文提出并验证两个假设：
  1. **适当变换可恢复错误空间预测**：几何图像变换与查询反转可提供互补推理视图，使直接推理错误的样本有机会被正确回答。
  2. **正确预测关系 token 置信度更高**：正确变换预测通常比错误直接预测具有更高的关系 token 置信度。
- 验证实验：在 Qwen2-VL-7B、Controlled-B + CV-Bench-R 上，错误样本数 `n_err=141`，恢复样本数 `n_rec=130`，恢复率 `ρ_rec=92.20%`；其中 `n_conf=107`，置信度优势率 `ρ_conf=82.31%`。

### 2.2 关键技术细节与流程
- **输入定义**：给定图像 `I` 和问题 `Q`，VLM 参数为 `θ`，生成回答 `y ~ pθ(· | I, Q)`。
- **Stage I：空间查询筛选**
  - 使用轻量语言模型 `gφ` 对问题做二分类：`spatial` 或 `non-spatial`。
  - 若为 `non-spatial`，直接使用原始输入查询 VLM，避免不必要变换。
  - 实验中使用 Qwen2.5-1.5B-Instruct 作为分类器。
- **Stage II：变换与置信度路由**
  - 对空间问题构造变换集合 `T = T_I ∪ T_q`。
  - `T_I` 为几何图像变换集合，实验中固定为水平翻转 `H` 和垂直翻转 `V`，大小记为 `M`。
  - `T_q` 为查询反转变换集合，交换目标实体与参考实体，大小记为 `N`；例如将“mug 相对 knife 在哪”反转为“knife 相对 mug 在哪”。
  - 直接推理分支记为 `k=0`，变换分支为 `k=1,...,M+N`。
  - 每个分支由 VLM 生成回答 `y_k`，抽取预测空间关系标签 `ℓ_k`，再映射回原始答案空间，记为 `ℓ*_k`。
  - 关系 token 置信度定义为：
    - `c_k = min_{t∈P_k} log pθ(y_{k,t} | y_{k,<t}, I_k, Q_k)`
    - 其中 `P_k` 是构成关系标签的 token 位置集合，取最小 token log-prob 作为
