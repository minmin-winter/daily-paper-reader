---
title: "LoMo: Local Modality Substitution for Deeper Vision-Language Fusion"
title_zh: LoMo：面向更深层视觉-语言融合的局部模态替换
authors: "Feng Han, Zhixiong Zhang, Zheming Liang, Yibin Wang, Jiaqi Wang"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30265v1"
tags: ["query:balanced-mml"]
score: 10.0
evidence: 针对训练中文本-图像角色不对称导致的载体敏感性问题，提出局部模态替换以深化融合
tldr: 现有视觉语言模型存在“载体敏感性”问题，将文本替换为渲染图像会导致性能骤降，根源在于训练数据中文本与图像角色不对称。我们提出LoMo，一种轻量级数据策展范式，通过动态将文本片段渲染为图像构建交错多模态序列，强制跨模态语义不变性。在13个基准测试中，LoMo带来一致增益，LLaVA-OneVision-1.5-8B提升2.67点，Qwen3.5-9B提升2.82点，实现了更深层的视觉-语言融合。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30265v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1422, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1459, \"height\": 648, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30265v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1449, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 736, \"height\": 703, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1169, \"height\": 376, \"label\": \"Table\"}]"
motivation: 由于训练数据偏置，视觉语言模型对文本和图像载体存在不对称偏好，导致模态替换时推理脆弱。
method: LoMo动态选择文本跨度并渲染为图像，重构交错多模态序列以监督跨模态表示不变性。
result: 在13个多模态基准上，LoMo一致提升性能，LLaVA-OneVision-1.5-8B和Qwen3.5-9B分别提高2.67和2.82点。
conclusion: LoMo通过轻量数据策展实现更深层融合，有效缓解载体敏感性，提升多模态推理的鲁棒性。
---

## 摘要
视觉-语言模型（VLMs）在大规模图文训练驱动的多模态融合基础上，已在广泛的理解与推理任务中取得显著进展。理想情况下，用文本问题的渲染图像版本替换原始文本应基本不影响模型性能。然而实践中，这种模态替换会导致性能大幅下降。我们将此“载体敏感性”问题归因于当前训练语料的内在偏差。在图像描述、视觉问答、光学字符识别以及网络来源的交错数据等常见数据集中，文本和图像通常被组织成截然不同且不对称的角色，文本充当语言查询，图像作为视觉参照。这种数据偏差导致视觉-语言模型在不同模态之间表现出明显的信息获取偏好。因此，视觉-语言模型无法在文本和视觉载体之间对齐语义等价内容的表征，使得模型推理在模态替换时变得脆弱。为解决该问题，我们提出了局部模态替换（LoMo），一种轻量级、与架构无关的数据整理范式，旨在为语义等价的文本与图像载体之间提供跨模态表征不变性监督。LoMo通过将单模态提示重构为无缝交错的多模态序列来实现这一目标，动态选择目标文本片段并将其转化为渲染图像，从而在“文本、视觉、文本”载体中保留相同的语义。在13个多样化多模态基准上的广泛实验表明，LoMo显著改善了整体多模态推理能力，并实现了更深层的跨模态融合。具体而言，它在基础模型上取得了持续提升，在LLaVA-OneVision-1.5-8B上比标准监督微调提高了2.67分，在Qwen3.5-9B上提高了2.82分。

## Abstract
Vision-Language Models (VLMs) have achieved substantial progress across a wide range of understanding and reasoning tasks, driven by large-scale image-text training aimed at multimodal fusion. Ideally, replacing a textual question with its rendered-image counterpart should leave model performance essentially unaffected. In practice, however, such modality substitution induces dramatic performance degradation. We attribute this "carrier sensitivity" issue to an inherent bias in current training corpora. Across prevalent datasets such as image captioning, VQA, OCR, and web-sourced interleaved data, text and images are typically organized into distinct and asymmetric roles, with text serving as linguistic queries and images as visual references. Such data bias leads VLMs to exhibit distinct preferences for information acquisition across different modalities. Consequently, VLMs fail to align representations of semantically equivalent content across textual and visual carriers, making model reasoning fragile under modality substitution. To address this, we propose Local Modality Substitution (LoMo), a lightweight, architecture-agnostic data curation paradigm designed to provide supervision for cross-modal representational invariance between semantically equivalent text and image carriers. LoMo achieves this by reformulating single-modality prompts into seamlessly interleaved multimodal sequences. It dynamically selects target text spans and recasts them as rendered images, thereby preserving the same semantics across "text, visual, text" carriers. Extensive experiments across 13 diverse multimodal benchmarks demonstrate that LoMo significantly improves overall multimodal reasoning and yields deeper cross-modal fusion. Specifically, it delivers consistent gains across foundational models, improving over standard SFT by 2.67 points on LLaVA-OneVision-1.5-8B and 2.82 points on Qwen3.5-9B.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

本论文《LoMo: Local Modality Substitution for Deeper Vision-Language Fusion》揭示了当前主流视觉-语言模型（VLMs, Vision-Language Models）中普遍存在的 **“载体敏感性”（Carrier Sensitivity）问题**。

-   **现象定义**：当模型接收到多模态查询时，如果将该查询中的文本部分替换为包含完全相同语义内容的渲染图像，模型的性能会遭到显著且持续的退化。
-   **问题归因**：作者将此现象归因于**模态鸿沟（Modality Gap）**，其根源在于当前预训练数据中存在**内在偏差（Inherent Bias）**。
-   **偏差本质**：在图像描述、视觉问答、OCR等典型训练语料中，文本和图像扮演着明显不对称的角色：文本通常作为语言指令或查询，而图像主要作为视觉参考或证据。这导致视觉-语言模型习得了一种对不同模态载体的信息获取偏好，无法为语义等价但模态不同的内容（纯文本 vs. 渲染文本图像）建立一致的跨模态表示。
-   **研究证据**：论文通过实验（图1(b)）证明，性能下降的幅度与文本和其渲染图像版本之间的**配对跨模态距离（Pairwise Cross-Modal Distance）** 呈严格的单调递增关系，这表明表示层面的不一致是性能脆弱的直接原因。
-   **研究目标**：论文旨在通过一种轻量级的数据策展方案，在现有训练范式中引入隐式的跨模态对齐监督信号，从而弥合模态鸿沟，增强视觉-语言模型的跨模态融合能力与鲁棒性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

论文提出了一种名为 **局部模态替换（LoMo）** 的、与模型架构无关的数据策展范式。其核心思想是，在不改变模型结构、不引入额外推理开销的前提下，通过重构训练数据的格式，迫使模型学习将“文本符号”和“像素化文本”视为语义上可互换的载体。

LoMo 的工作流程包含三个顺序阶段，将一个纯文本实例 $(x, a)$ 转换为一个图文交错的实例 $(T(x), a)$，其中 $x$ 是问题，$a$ 是答案。

-   **核心阶段**:
    1.  **结构感知的文本片段定位：**
        -   对于短句（句数 ≤ 3），直接将整个文本作为待替换的目标片段 $x_{mid}$。
        -   对于长句，首先进行**公式感知的分块**，将数学表达式和 LaTeX 命令视为不可分割的原子单元，然后提取序列的中间三分之一部分作为 $x_{mid}$，确保截断边界不会出现在公式内部。剩余的上下文则成为前缀 $x_{pre}$ 和后缀 $x_{suf}$。

    2.  **视觉渲染：**
        -   根据 $x_{mid}$ 的内容特征，通过一个**内容感知的路由机制**将其转换为图像。
        -   包含数学公式的片段被路由至 LaTeX 渲染器，纯文本片段则使用标准文本渲染器，并采用失败回退机制保证数据产出率。
        -   生成的图像 $I$ 会进行边缘裁剪以优化尺寸。

    3.  **感知失真：**
        -   对渲染图像 $I$ 施加保留语义的、模拟真实世界场景的退化操作，得到最终的视觉载体 $I^\prime$。
        -   失真操作集合包括：**旋转（Rotate）**、**模糊（Blur）**、**阴影/污渍（Shadow-or-stain）** 和 **褶皱（Wave）**。
        -   最终，用 $I^\prime$ 替换原始的 $x_{mid}$，形成“$x_{pre}$ → 图像 → $x_{suf}$”的图文交错序列。

-   **核心机制与公式推导**：
    LoMo 将标准的监督微调（SFT）目标由公式 (4) 的 $L_{SFT}(\theta; x, a) = -\log p_\theta(a | x)$ 重构为公式 (5) 的形式。通过数学推导（公式6），论文证明优化 LoMo 的目标函数等价于在标准 SFT 损失的基础上，增加了一个隐式的**跨模态对齐项**：
    $$E_{a \sim p_\theta(\cdot | x)}[-\log p_\theta(a | T(x))] = E_{a \sim p_\theta(\cdot | x)}[-\log p_\theta(a | x)] + \text{KL}\big(p_\theta(\cdot | x) \,\|\, p_\theta(\cdot | T(x))\big)$$
    这个 KL 散度项迫使模型在给定纯文本载体 $x$ 和给定多模态交错载体 $T(x)$ 时，其预测分布趋于一致，从而实现了跨模态表征不变性的监督。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

-   **基座模型**:
    -   LLaVA-OneVision1.5-8B-Base
    -   Qwen3.5-9B-Base
    选择这两个架构不同的模型旨在验证方法的架构无关性。

-   **训练数据**:
    -   训练数据池统一从 LLaVA-OneVision1.5 的 SFT 语料库中随机采样，包含 2M 多模态指令数据 和 2M 纯文本指令数据。

-   **评价基准与协议**:
    -   **评价基准**：覆盖 6 大类别、共 13 个多模态基准测试，包括：通用推理（MMMU, MMMU-Pro）、数学推理（MathVista, ZeroBench, WeMath）、事实性（SimpleVQA, HallusionBench）、指令遵循（MM-IFEval）、文档/OCR（MMLongBench-Doc, DocVQA, CC-OCR）和视觉感知（V*, CountBench）。
    -   **双重评价协议**：
        1.  **标准评价**：使用原始的（图像 + 文本问题）输入。
        2.  **渲染评价**：将整个文本问题渲染为单张图像，与原始图像一同输入模型，以直接测试载体敏感性。
    -   **内在对齐指标**：
        1.  **模态集成率（MIR）**：度量模型内部视觉和文本词元分布间的整体差值。
        2.  **配对跨模态距离（Pairwise Cross-Modal Distance）**：度量成对的文本和渲染图像在表示空间中的样本级距离。

-   **对比方法**:
    -   **标准 SFT（Standard SFT）**：在相同的数据池（4M 原始数据）上直接进行微调，作为基线。
    -   **全文本渲染（Full-Text Rendering）**：直接将整个问题渲染为图像进行训练，作为消融对比。
    -   **LoMo 变体**：包括不含感知失真的 LoMo、不同重写比例、不同渲染位置的 LoMo 等。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

-   **渲染计算资源**：LoMo 的数据构造流水线（图像渲染）在两个具有 128 核心的 CPU（中央处理器）服务器上进行，耗时约 20 小时。
-   **模型训练与评估资源**：所有模型的训练和评估实验均在单个节点上完成，该节点配备了 **8 块 NVIDIA H200 GPU（图形处理器）**。文中未明确说明总训练时长或总 GPU 小时数。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

论文进行了非常全面且详尽的实验，数量多、维度广，说服力强。

-   **主实验**：在两个不同架构的基座模型、13个基准测试、两种评价协议下进行了全面对比（共产生了 $2 \times 13 \times 2 = 52$ 项主要性能指标对比，见表1），结果非常一致。
-   **组件消融实验（表2）**：系统性地验证了结构感知片段定位和感知失真两大组件的独立贡献。例如，直接进行全文本渲染效果甚微（+1.19），而 LoMo（无失真）则提升显著（+2.22），证明片段定位是主要贡献者，而失真则带来进一步提升（+2.68）。
-   **训练数据规模消融（图4）**：在 1M, 2M, 3M, 4M 四个数据规模下，同时跟踪下游精度和两个内在对齐指标，结果表明 LoMo 的优势随数据量增加而持续扩大，并非仅在特定预算下有效。
-   **关键参数消融**：
    -   **重写比例（表3）**：研究了 0%, 25%, 50%, 75%, 100% 五种比例，发现 50% 为最佳，过高或过低都会导致收益饱和或下降。
    -   **渲染位置（表4）**：比较了前缀、中缀（LoMo默认）、后缀和多段渲染四种结构，证明中缀的“文本-图像-文本”结构最能促进跨模态融合。
-   **公平性控制实验（表5）**：为了排除 LoMo 的增益是来自“更多含图样本”这一简单假说，作者通过重采样，将 LoMo 与标准 SFT 的图文/纯文本样本比例严格对齐（1:1）。在此公平条件下，LoMo 仍取得 +2.45 的显著提升，证明其方法本身（而非数据比例）是优势来源。
-   **纯文本能力测试（表6）**：在校验模型纯文本能力时，LoMo 在 5 个纯文本基准上表现与标准 SFT 持平甚至略有提升，证明了其增强多模态能力的同时无损文本能力。

整体来看，实验设计极为细致、客观且公平，全方位验证了方法的有效性、各组件的作用以及其鲁棒性。

### 6. 论文的主要结论与发现

1.  **问题确认**：现代视觉-语言模型存在严重的“载体敏感性”，同样的语义内容通过文本或图像形式输入会导致巨大性能差异，其根本原因是训练数据中文本和图像角色的不对称性造成的模态鸿沟。
2.  **方法有效性**：提出的 LoMo 范式通过简单的数据策展，将标准 SFT 目标转化为隐式的跨模态对齐信号，成功促使模型学习到语义等价的跨模态表示不变性。
3.  **性能提升显著**：
    -   **标准评价下**，LoMo 在两个基座模型上平均提升 **2.68** 和 **2.82** 分。
    -   **渲染评价下**，LoMo 的增益急剧扩大，分别达到 **18.86** 和 **11.92** 分，极大弥合了标准与渲染评价之间的性能鸿沟。
4.  **内在对齐提升**：LoMo 能够持续且显著地降低 MIR 和配对跨模态距离，从表示层面证实了其对更深层视觉-语言融合的促进作用。
5.  **架构与规模鲁棒性**：方法不依赖模型架构，在不同规模的训练数据和不同重写比例下均表现稳健。

### 7. 优点：方法或实验设计上有哪些亮点

-   **深刻的问题洞见**：从“载体敏感性”这一直观现象出发，通过量化分析将其与“模态鸿沟”这一深层原因紧密关联，问题定位精准且有说服力。
-   **巧妙的方法论设计**：
    -   **轻量级与无侵入性**：LoMo 是一种纯粹的数据策展方案，无需修改模型架构，无推理开销，兼容任何多模态训练流程，极具实用价值。
    -   **隐式监督**：通过巧妙的数据重构将“跨模态对齐”从显式的训练目标转化为标准 SFT 任务的一部分，机制优雅。
    -   **三阶段流水线**：结构感知定位、内容感知渲染和感知失真的组合设计考虑周全，确保生成数据的质量和真实感。
-   **极致的实验严谨性**：
    -   **双重评价协议**：标准与渲染评价的结合是验证该方法核心价值（解决载体敏感性）的绝佳设计。
    -   **多维度消融**：对方法的各组件、关键超参（重写比例、渲染位置）、数据规模进行了非常详尽的探索。
    -   **严格的公平性控制**：控制图文样本比例这一消融实验排除了最常见的混淆因素，是该工作的亮点之一。
    -   **内在表征分析**：使用 MIR 和配对距离等指标，从模型内部表征层面提供了证据，使得结论更具深度。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

1.  **训练阶段单一**：LoMo 的效果仅在 SFT 阶段得到验证。其在预训练阶段或基于人类反馈的强化学习（RLHF）等后训练阶段的应用效果尚待探索。
2.  **片段定位策略相对基础**：当前使用的块级中间三分之一启发式策略较为简单。未来可能需要探索难度感知的、课程式的或更细粒度的选择策略以获得进一步提升。
3.  **模型规模验证有限**：受限于计算资源，实验仅在 8B 和 9B 规模的模型上进行。该方法在显著更大规模（如数十、上百B参数）的视觉-语言模型上的可扩展性尚未被证实。
4.  **继承性偏差风险**：作为一种构建在已有预训练视觉-语言模型之上的方法，LoMo 无法消除其基座模型在预训练阶段引入的内在偏见和失败模式，部署时需遵循现有安全准则。

（完）
