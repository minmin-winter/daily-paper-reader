---
title: Learning Context-Conditioned Predicate Semantics via Prototype Feedback
title_zh: 通过原型反馈学习上下文条件的谓词语义
authors: "NamGyu Jung, Chang Choi"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29610v1"
tags: ["query:mm-reasoning"]
score: 8.0
evidence: 用于场景图生成的多模态视觉语言推理
tldr: "场景图生成中谓词的多义性难以建模，现有方法使用静态原型或检索相似样本，但无法根据图像证据动态调整语义。本文提出AlignG，通过从候选关系中推断上下文相关的谓词语义，并利用原型反馈机制重新校准关系表示，同时锚定全局语义中心防止漂移。在VG-150和GQA-200上，SGDet任务F@100分别提升1.4和2.7。方法实现了原型的选择性合并与分离，展现了上下文依赖的语义重组能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29610v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1770, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29610v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1769, \"height\": 838, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29610v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 861, \"height\": 1509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29610v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1743, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29610v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1776, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29610v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1777, \"height\": 353, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29610v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1769, \"height\": 649, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1768, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1766, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 875, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 822, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1065, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29610v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1433, \"height\": 507, \"label\": \"Table\"}]"
motivation: 现有谓词表示静态，无法根据图像特定证据动态重组语义，导致歧义场景下系统混淆。
method: AlignG从图像中的关系候选推断上下文相关的谓词语义，并通过原型反馈机制重新校准关系表示，同时锚定全局语义中心。
result: "在VG-150和GQA-200的SGDet任务上，F@100分别提升+1.4和+2.7，超越先前最佳方法。"
conclusion: AlignG通过原型反馈实现上下文条件化的谓词语义适应，有效缓解多义性，提升场景图生成性能。
---

## 摘要
在场景图生成中，一个核心挑战是对多义谓词进行建模，这些谓词的含义会随上下文而变化。先前的方法通过将谓词分解为多个静态原型或检索语义相似的示例来解决这个问题。然而，这些策略使谓词表示保持静态，无法重新组织语义以反映特定于图像的证据，从而在模糊上下文中导致系统性混淆。我们提出了AlignG，它通过原型反馈学习上下文条件的谓词语义。AlignG从每张图像中的关系候选中推断出上下文条件的谓词语义，并将调整后的语义反馈回来以重新校准关系表示。学习目标将该调整锚定到全局语义中心，在场景提供一致的关系线索时，既防止语义漂移，又允许选择性重组。在VG-150和GQA-200上的实验表明，与最先进的基线相比，在SGDet设置下，F@100分别提高了+1.4（VG-150）和+2.7（GQA-200）。我们进一步可视化了每张图像的相似度变化，并观察到一致的上下文依赖重组，其中原型根据场景证据选择性地合并或分离谓词。代码可在https://github.com/Namgyu97/AlignG-SGG.pytorch获取。

## Abstract
In scene graph generation, a central challenge is modeling polysemous predicates whose meanings shift across contexts. Prior approaches address this issue by decomposing predicates into multiple static prototypes or retrieving semantically similar exemplars. However, these strategies keep predicate representations static and cannot reorganize semantics to reflect image-specific evidence, leading to systematic confusions in ambiguous contexts. We propose AlignG, which learns context-conditioned predicate semantics via prototype feedback. AlignG infers context-conditioned predicate semantics from the relation candidates within each image and feeds the adapted semantics back to recalibrate relation representations. The learning objective anchors this adaptation to global semantic centers, preventing semantic drift while still allowing selective reorganization when the scene provides consistent relational cues. Experiments on VG-150 and GQA-200 show consistent improvements over state-of-the-art baselines, with F@100 improvements of +1.4 on VG-150 and +2.7 on GQA-200 under SGDet. We further visualize per-image prototype similarity shifts and observe coherent context-dependent reorganization where prototypes selectively merge or separate predicates according to scene evidence. The code is available at https://github.com/Namgyu97/AlignG-SGG.pytorch.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：场景图生成（SGG）中，谓词（predicate）具有多义性，同一标签（如“on”）在不同上下文中含义不同（空间接触 vs. 功能性使用）。现有方法（如PE-Net、C-SGG、RA-SGG）使用静态原型或检索示例，但无法根据图像特定证据动态重组语义，导致歧义场景下系统性混淆。
- **研究背景**：场景图生成旨在将图像表示为对象及语义关系的图结构，支撑视觉问答、图像检索等下游任务。谓词的长尾分布和多义性是主要障碍。先前工作通过分解谓词为多个簇、引入外部知识或检索示例来缓解，但原型一旦训练完成便固定，无法适应新图像中的上下文证据。
- **论文贡献**：提出**AlignG**，通过原型反馈（prototype feedback）学习上下文条件化的谓词语义，在保持全局语义结构的同时，允许依据图像证据选择性重组谓词原型。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- 将谓词学习重新定义为**图像条件适应问题**：构建两阶段交互机制，通过跨注意力（cross-attention）实现全局原型与局部关系嵌入之间的双向信息流，最终用适应后的原型反馈来重新校准关系表示。

### 关键技术细节
1. **上下文化谓词原型（Contextualizing Predicate Prototypes）**
   - 对每个静态原型 \(\bar{p}_r\)，通过兼容性加权的跨注意力聚合关系嵌入 \(e_j\)，得到上下文向量 \(u_r\)（公式1）。
   - 使用**GRU**单元将 \(u_r\) 与归一化后的静态原型融合，生成图像适应原型 \(p_r^{(I)}\)（公式2）。GRU的门控机制选择性地整合有用信息，防止突兀的语义漂移。

2. **重新校准关系嵌入（Recalibrating Relation Embeddings）**
   - 对每个关系嵌入 \(e_j\)，通过反向跨注意力（公式3）从所有适应原型中聚合指导向量 \(u_j\)。
   - 将 \(e_j\) 与 \(u_j\) 拼接后通过投影网络 \(f_{proj}\) 得到校准后的表示 \(\tilde{e}_j\)（公式4），实现单步有效适应。

3. **训练目标与原型适应**
   - **正则化损失** \(L_{reg}\)（公式5）：包含相似性惩罚项和多样性约束，防止原型冗余，鼓励语义空间覆盖。
   - **对比对齐损失** \(L_{align}\)（公式6）：将校准后的关系嵌入 \(\tilde{e}_j\) 拉向正确静态原型，推远最混淆的负原型。注意这里使用**静态原型**而非适应原型，以避免共适应，保持全局中心。
   - 总损失 \(L = L_{cls} + L_{reg} + L_{align}\)，其中 \(L_{cls}\) 为交叉熵分类损失。

### 算法流程（文字描述）
- 输入图像 → 通过Faster R-CNN检测对象和关系候选。
- 对每个图像：获取所有关系嵌入 \(e_j\)；使用跨注意力计算每个静态原型 \(\bar{p}_r\) 的上下文信号，经GRU生成适应原型 \(p_r^{(I)}\)；再用反向跨注意力从适应原型聚合关系指导向量，对每个关系嵌入进行校准得到 \(\tilde{e}_j\)。
- 用校准后的 \(\tilde{e}_j\) 进行分类并计算损失，梯度反向传播同时更新跨注意力参数、GRU和投影网络。

## 3. 实验设计
### 数据集与场景
- **VG-150**：Visual Genome的常用子集，150个对象类别，50个谓词类别，每图平均38对象、22关系，长尾分布显著。
- **GQA-200**：GQA的SGG子集，200个对象类别，100个谓词类别，强调组合推理与细粒度关系。

### 评估设置
- 三种任务：Predicate Classification (PredCls)、Scene Graph Classification (SGCls)、Scene Graph Generation (SGDet)。SGDet最具挑战性。
- 指标：Recall@K (R@K)、Mean Recall@K (mR@K)、F@K（调和平均），后者近年被强调为平衡全局召回与类别公平性的指标。
- 实验分为标准训练和频率感知长尾训练（标注†），后者使用数据集级谓词频率/共现统计进行重加权。

### 对比方法
- **既往方法**：KERN、BGNN、HetSGG、SQUAT、CooK、Motifs、TDE、NICE、IETrans、CFA、ST-SGG、DPL、SSC-SGG等。
- **基于原型的方法**：PE-Net（骨干）、MCL（多概念学习）、RA-SGG（检索增强）。
- **其他**：VTransE、VCTree、DPL等也在GQA-200上对比。

### 主要结果
- **VG-150**（表1）：AlignG†在PredCls上mR@100 42.6（+0.2 vs MCL†），SGCls上26.1（+1.3），SGDet上19.7（+2.4）；F@100在SGDet上达到23.8（+1.4）。
- **GQA-200**（表2）：AlignG†在PredCls上mR@100 37.9（+1.1 vs RA-SGG†），SGCls上17.4（+0.2），SGDet上15.5（+0.5）；F@100在SGDet达19.5（+2.7 vs RA-SGG† ±10.8 mR@100 over PE-Net）。
- 消融实验（表3）验证各组件贡献，GRU更新优于拼接、残差、EMA等。

## 4. 资源与算力
- **GPU型号**：文中明确提到“实验在RTX 4090 GPUs上运行”，但未说明具体数量（可能为单卡或多卡）。
- **训练时长**：训练60k迭代，batch size 8；推理速度大于30 FPS（表4显示AlignG推理30.14 FPS vs PE-Net 30.84 FPS，下降可忽略）。
- **计算开销**：相比PE-Net（472.36G FLOPs），AlignG仅增加7.05G FLOPs（479.41G），每迭代训练时间增加0.03秒（0.35→0.38 s/iter），开销极小。

## 5. 实验数量与充分性
- **主实验**：两个数据集（VG-150、GQA-200）× 三种任务 × 两种设置（标准/长尾），共12组对比，覆盖全面。
- **消融实验**：
  - 组件消融（表3）：逐部件添加（边缘更新、拼接原型、GRU原型），并对比有无重加权。
  - 更新机制对比（附录B）：GRU vs Identity、Residual、Plain Addition、EMA，共5种。
  - 损失加权敏感性（附录C）：9组系数组合。
  - 场景密度鲁棒性（附录D）：按图像关系数量分4个密度桶。
  - 谓词级分析（附录A，图4）：50个谓词独立比较。
  - 混淆分析（表5）：5类典型错误对解决率。
- **定性分析**：图3展示3张图像的原型相似度变化热图；附录E展示4个成功/失败案例（图5-6）。
- **充分性**：实验设计较为充分，覆盖了主要任务、指标、变体及边界情况。所有对比均使用标准流程，消融系统，结论可靠。但未在更多数据集（如OpenImages）上验证，也未与最新端到端Transformer模型（如SGTR、EGTR）在相同骨干下对比（但已在统一骨干下与PE-Net对比公平）。

## 6. 主要结论与发现
- AlignG通过原型反馈有效实现了上下文条件化的谓词语义适应，在保持全局语义结构的同时，能依据图像证据选择性合并或分离原型。
- 在VG-150和GQA-200上，AlignG在mR@100和F@100上均取得SOTA，尤其在SGDet设置下F@100提升明显（+1.4和+2.7），证明其对长尾和歧义场景的鲁棒性。
- 消融证实GRU门控更新是关键：相比线性融合，GRU选择性过滤噪声，避免语义漂移，实现高性能平衡。
- 原型反馈机制计算开销极低，附加FLOPs不足2%，推理速度保持实时。
- 谓语级分析表明：边缘更新利于强局部几何线索的谓词，原型反馈利于依赖整体场景上下文的谓词（如“painted on”“says”），两者互补。

## 7. 优点
- **方法创新性**：首次将谓词学习视为图像条件适应问题，两阶段跨注意力+GRU反馈的设计巧妙且必要，区别于静态原型范式。
- **性能与效率平衡**：仅增加极小开销即带来显著提升，适合实际部署。
- **消融系统全面**：从组件、更新机制、损失权重、密度鲁棒性等多角度验证，支撑结论。
- **可视化直观**：原型相似度热图显示了语义重组过程，定性案例解释了成功与失败模式，有助于理解模型行为。
- **公平性**：在标准基准上对比大量最新方法，且区分标准与长尾训练，控制变量清晰。

## 8. 不足与局限
- **实验覆盖**：仅在VG-150和GQA-200上验证，未考虑OpenImages等其他SGG数据集，泛化性有待拓展。
- **数据集偏差风险**：作者在Impact Statement中指出，AlignG继承Visual Genome和GQA的标注偏见，上下文适应可能强化虚假相关性，在安全敏感应用需谨慎。
- **失败案例分析**（附录E）：模型可能被诱导性上下文误导（如手靠近领带被误判为“holding”），或过度关注静态视觉线索而忽略动作语义（如滑板场景误判为“standing on”）。这表明原型反馈对强噪声或矛盾上下文仍不够鲁棒。
- **理论基础**：GRU更新和跨注意力机制缺乏严格的收敛性或稳定性分析，属于工程实现。
- **与其他SOTA比较**：在SGDet任务上R@100略低于某些方法（如R@100=30.0 vs RA-SGG的30.3，但F@100更高），说明存在召回-公平性权衡，但论文已使用F@100作为平衡指标。
- **未与最先进Transformer模型（如SGTR）同骨干比较**：尽管AlignG以PE-Net为骨干，但如今SGG领域已发展出端到端Transformer方法，缺乏这类对比可能无法完全体现方法的上限。

（完）
