---
title: "Segmentation before Answering: Pixel Grounding for MLLM Visual Reasoning"
title_zh: 先分割后回答：面向多模态大语言模型视觉推理的像素级定位
authors: "Yake Wei, Yuan Wang, Fengyun Rao, Jing Lyu, Di Hu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05798v1"
tags: ["query:mm-reasoning"]
score: 10.0
evidence: 像素级分割提升MLLM视觉推理能力
tldr: 多模态大语言模型（MLLM）正从静态感知发展为交错视觉语言推理，其中关键操作是对感兴趣区域进行放大以获取细节。现有方法多采用边界框，但易受冗余背景和干扰物体影响。本文提出SegAnswer，将放大单元从边界框替换为像素级分割掩码，通过精细掩码隔离目标区域，过滤冗余信息，且分割后的离散图像块与MLLM的位置嵌入更兼容。实验表明，SegAnswer在高分辨率感知、通用感知和幻觉检测等基准上取得一致提升，并展现出可靠的像素级接地能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05798v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 1341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1700, \"height\": 1421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1729, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05798v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 817, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1865, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1696, \"height\": 481, \"label\": \"Table\"}]"
motivation: 现有MLLM视觉推理中基于边界框的区域放大易受冗余背景和干扰物体影响，限制推理精度。
method: 提出SegAnswer，以像素级分割掩码替代边界框作为放大单元，过滤冗余信息，使视觉输入与位置嵌入对齐。
result: 在高分辨率感知、通用感知和幻觉检测等基准上取得一致提升，展现可靠的像素级接地性能。
conclusion: 证明像素级分割接地能有效提升MLLM视觉推理能力，为精细视觉理解提供新范式。
---

## 摘要
多模态大语言模型（MLLM）的最新进展已从静态感知演变为交织的视觉语言推理，常被称为“用图像思考”。在该推理过程中，一个基本操作是放大感兴趣的区域（通常用边界框表示）以获取更精细的视觉细节。本文提出先分割后回答（SegAnswer），将放大单位从流行的边界框转变为像素级分割掩码。通过使用细粒度掩码将目标区域与杂乱环境隔离，分割后的视觉输入能产生更精确的感兴趣区域，有效过滤冗余背景和干扰物体。此外，分割后视觉输入的离散块与MLLM通过位置嵌入结构化视觉令牌的方式更加契合。实验中，我们在多个基准（包括高分辨率感知、通用感知和幻觉）上评估了SegAnswer。它取得了一致的改进，并在分割任务上表现出可观性能，验证了其可靠的像素级定位能力。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have evolved from static perception to interleaved visual-language reasoning, often referred to as ``thinking with images''. A basic operation in this reasoning process is to zoom in on regions of interest (often represented with bounding boxes) to acquire finer visual details. In this paper, we propose \textbf{Seg}mentation before \textbf{Answer}ing (SegAnswer), which shifts the unit of zoom-in from the popular bounding box to pixel-level segmentation mask. By employing fine-grained masks to isolate the target area from cluttered environments, segmented visual input yields a more precise region of interest, effectively filtering out redundant background and interfering objects. Furthermore, the discrete patches of segmented visual input align more seamlessly with how MLLMs structure visual tokens via positional embeddings. In experiments, we evaluate SegAnswer across diverse benchmarks, including high-resolution perception, general perception, and hallucination. It achieves consistent improvements and also exhibits considerable performance on segmentation tasks, validating its capability for reliable pixel grounding.

---

## 论文详细总结（自动生成）

```markdown
# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：多模态大语言模型（MLLM）在视觉推理中常采用“放大感兴趣区域”操作获取细节，现有方法普遍使用**边界框（Bounding Box, BBox）** 来定位该区域。但BBox存在两个固有缺陷：① 物体形状不规则时，矩形框会引入大量冗余背景，浪费计算资源；② 物体相互遮挡时，BBox难以将目标从相邻实体中解耦，导致语义干扰。这些问题本质上是BBox粒度过粗，无法精准隔离感兴趣区域。
- **核心贡献**：提出**SegAnswer**（Segmentation before Answering），将放大单元从边界框升级为**像素级分割掩码（segmentation mask）**，通过精确的像素接地（pixel grounding）消除视觉噪声，保留纯净的目标区域，从而提升MLLM的视觉推理能力。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将视觉推理中的“放大”操作从矩形裁剪改为精细分割，使模型能够生成目标物体的完整掩码，并用该掩码切割出的图像块（保留原图位置索引）作为后续推理的新视觉输入。
- **技术细节**：采用**三阶段训练**流程：
  1. **Stage 1：像素接地**  
     - 基于Qwen2.5-VL-7B，集成SAM 2.1作为掩码解码器，通过额外MLP投影器连接MLLM隐藏状态与掩码解码器。  
     - 新增特殊令牌`<|seg|>`，当模型生成该令牌时，提取其隐藏状态通过投影器输入SAM 2.1解码出掩码。  
     - 训练目标：下一令牌预测损失 + 分割损失（focal loss + dice loss + IoU损失 + 物体分类损失）。  
     - 两阶段训练：先冻结MLLM和掩码解码器，仅训练投影器；再使用LoRA微调MLLM，并与投影器、掩码解码器联合更新。
  2. **Stage 2：多模态交织监督微调**  
     - 构建对话模板，模型先判断是否需要分割，若需要则输出`<|seg|>`，获得分割图像块后结合原始图像回答。  
     - 分割图像块保留原图中的位置索引，保持空间关系与位置嵌入兼容。  
     - 仅使用下一令牌预测损失，不监督分割掩码；冻结投影器和掩码解码器，全参数微调MLLM。
  3. **Stage 3：强化学习优化推理**  
     - 采用DAPO算法，将推理轨迹定义为文本和视觉历史的交替序列。  
     - 奖励设计：准确率奖励（答案是否正确）+ 格式奖励（是否规范使用`<|seg|>`、`<answer>`等标签），加权求和。  
     - 损失计算仅针对模型生成令牌，忽略观察输入。

## 3. 实验设计：数据集、Benchmark、对比方法
- **训练数据**：  
  - Stage 1：RefCOCO、RefCOCO+、RefCOCOg、RefClef、ReasonSeg、ADE20K、COCOStuff、Mapillary Vistas、PACO-LVIS、PASCAL-Part。  
  - Stage 2：VisualCOT（438k QA对，仅使用答案监督，忽略BBox标注）。  
  - Stage 3：ViRL39K（39k可验证QA对）。
- **Benchmark**：  
  - 高分辨率感知：V*、HR-Bench 4K、HR-Bench 8K。  
  - 通用感知：MMBench、VisuLogic、MMVP。  
  - 幻觉检测：POPE、HallusionBench。  
  - 分割评估：RefCOCO、RefCOCO+、RefCOCOg。
- **对比方法**：  
  - 视觉推理基线：LLaVA-OneVision-9B、Qwen2.5-VL-7B（基模型）、Pixel Reasoner、DeepEyes。  
  - 分割专门方法：LISA、Groundhog、LaSagnA、VideoLISA、VISA、Vitron。  
  - 其他V*方法：SEAL、DyFo、Chain-of-Focus（仅在V*上额外对比）。

## 4. 资源与算力
- **论文未明确说明**所使用的GPU型号、数量以及训练总时长（如多少张A100/训练多少天）。仅给出了各阶段的学习率、epoch数和batch设置的描述（Stage1：投影器训练5epoch，lr=1e-3；LoRA联合训练3epoch，lr=2e-5；Stage2：1epoch，lr=2e-6；Stage3：1epoch，lr=2e-5，rollout=4）。算力资源信息缺失。

## 5. 实验数量与充分性
- **实验数量**：主表（Table 1）覆盖8个benchmark（V*, HR-4K, HR-8K, MMBench, VisuLogic, MMVP, POPE, HallusionBench）；辅助表（Table 2）额外对比V*上的多个方法；分割测试表（Table 3）包含3个公认的接地分割数据集共6个subset；附带训练奖励曲线（Figure 4）及定性可视化（Figure 3）。
- **充分性分析**：  
  - **优势**：基准覆盖了高分辨率、通用、幻觉和分割等多种能力维度，对比方法既包括通用MLLM也包括视觉推理专用方法和分割专用方法，较为全面。  
  - **不足**：缺少“消融实验”——例如，若将分割替换为BBox在同一框架下（使用相同三阶段训练，但Stage1改为训练BBox预测）的性能对比，或移除Stage3 RL的对比，并未提供。因此无法直接归因提升完全来自分割而非其他设计（如RL）。此外，训练数据来源多样，但未对不同数据比例进行消融分析。

## 6. 论文的主要结论与发现
- **主要结论**：SegAnswer在各项视觉推理基准上均取得一致且显著的提升，尤其在高分辨率感知任务上改善最大（V*相比基模型提升+8.9，HR-4K +6.6，HR-8K +8.6）。  
- **分割能力验证**：即使在专注于分割任务的任务上，SegAnswer也超越了多个分割专用模型（如RefCOCO+ test-A达到78.7%），表明其像素接地质量高。  
- **RL训练有效**：Stage3的准确率奖励从0.35稳定升至0.55以上，格式奖励始终接近1，说明模型学会了规范使用接地操作并提升了答案正确率。

## 7. 优点
- **方法创新性**：首次系统性地用像素级分割替代边界框用于MLLM视觉推理中的区域放大，精准过滤冗余和干扰。  
- **位置索引保留**：分割后保留图像块在原图的位置索引，使稀疏块仍能反映空间关系，与MLLM的位置嵌入高度兼容。  
- **三阶段渐进训练**：从纯分割能力→多模态对话→RL优化，逐步提升，各阶段目标清晰，避免一次性学习困难。  
- **广泛的实验验证**：在多个不同类型benchmark上评估，并对比多种方法，证明了通用性和鲁棒性。

## 8. 不足与局限
- **计算资源不明**：未报告具体GPU型号、数量及训练时长，影响复现和公平比较（尤其是RL阶段计算需求较高）。  
- **缺乏深入消融**：未提供“分割 vs BBox”在相同框架下的消融、各阶段贡献的消融、数据量的消融，难以量化各模块的独立贡献。  
- **分割模块依赖**：分割质量依赖SAM 2.1的预训练能力，若目标物体与SAM训练数据分布差异大可能失效，文中未讨论失败案例。  
- **规模局限**：仅在7B模型上实验，未在更大参数规模（如70B）或不同架构上验证，泛化性有待考证。  
- **RL奖励设计简单**：仅依赖答案准确性和格式，未对分割本身的质量（如掩码IoU）给予直接奖励，可能鼓励“有用但不完美”的分割。  
- **评估偏差风险**：部分benchmark（如POPE）存在饱和现象，SegAnswer提升幅度较小（+1.8），可能受限于上限；幻觉评测可能无法充分反映接地操作对减少幻觉的真正贡献。

（完）
```
